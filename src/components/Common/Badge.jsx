const Badge = ({ text }) => {
  return (
    <span className="border-b border-white/10 px-0 py-2 text-sm uppercase tracking-[0.18em] text-[#C5C6C7]">
      {text}
    </span>
  );
};

export default Badge;
